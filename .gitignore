# dependencies
/node_modules
cloudflare-worker/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# test harness output (regenerated on every run)
scripts/test-output.txt
scripts/test-capture-output.txt
scripts/seed-simplify-output.txt
scripts/link-check-dry-run.txt

# claude code local state (scheduled tasks lock, per-user settings)
.claude/

# personal working notes — kept local, not part of the public history.
# Used offline to generate README summaries; never committed.
/CLAUDE.md
/docs/STATUS.md
/docs/CHANGELOG.md
/docs/LOG.md
